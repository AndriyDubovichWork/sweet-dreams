import { ReactNode } from 'react';
export type centeredProps = {
  children: ReactNode;
  className?: string;
  absolute?: boolean;
  vertically?: boolean;
  horizontally?: boolean;
  content?: boolean;
};
