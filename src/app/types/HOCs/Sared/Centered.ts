import { ReactNode } from 'react';
export type centeredProps = {
  children: ReactNode;
  className?: string;
  absolute?: boolean;
  verticaly?: boolean;
  horizzontaly?: boolean;
  content?: boolean;
};
